console.log()

`
  Para mensajes generales de registro de información.
  Se puede utilizar sustitución de cadena y argumentos adicionales con este método.
`

str.length

`
  La propiedad length representa la longitud de una cadena.
`

str.endsWidth('')

`
  El método endsWith() determina cuando una cadena termina
  con los caracteres de otra cadena, devuelve true o false según sea.
`
str.slide()

`
  El método slice() extrae una sección de una cadena y devuelve una cadena nueva.
`
str.split()

`
  El método split() divide un objeto de tipo String en un array (vector)
  de cadenas mediante la separación de la cadena en subcadenas.
`

str.join()

`
  El método join() une todos los elementos de un arreglo en una cadena.
`

str.charAt()

 `
  El método charAt() de String devuelve el carácter especificado de una cadena.
 `
str.startsWith()

`
  El método startsWith() indica si un string inicia con los caracteres de otro string,
  regresando true o false según sea el caso.
`

Math.round()

`
  Devuelve el valor de un número redondeado al número entero más cercano.
`

Math.floor()

`
  Devuelve el mayor entero menor que o igual a un número.
`

Math.random()

`
  Devuelve un número pseudo-aleatorio entre 0 y 1.
`

number.toFixed()

`
  El método toFixed() formatea un número usando notación de punto fijo.
`
